import test from 'node:test';import assert from 'node:assert/strict';
import {wager,plinkoResult,multipliers,coinResult} from '../server/arcade.mjs';
import api from '../api/arcade.js';
process.env.KICK_REDIRECT_URI='https://www.zeekfusion.com/api/kick/callback';
const response=()=>({statusCode:200,setHeader(){},status(s){this.statusCode=s;return this},json(b){this.body=b;return this},end(){return this}});
test('wager bounds reject fractions, coercion, overflows and invalid amounts',()=>{for(const x of [0,-1,.5,1.01,10001,1000001,NaN,Infinity,'1e2',null,{},''])assert.throws(()=>wager(x));assert.equal(wager(1),1);for(const kind of ['plinko','higher','mines','blackjack']){assert.equal(wager(10000,kind),10000);assert.throws(()=>wager(10001,kind));}for(const kind of ['coin','dice','rps']){assert.equal(wager(1000001,kind),1000001);assert.throws(()=>wager('100',kind));}});
test('all 256 Plinko paths produce the published distribution and exact expected return',()=>{const counts=Array(9).fill(0);for(let i=0;i<256;i++)counts[i.toString(2).split('').filter(b=>b==='1').length]++;assert.deepEqual(counts,[1,8,28,56,70,56,28,8,1]);assert.equal(counts.reduce((sum,n,i)=>sum+n*multipliers[i],0)/256,251/256);});
test('server outcomes always match their paths and known slots',()=>{for(let i=0;i<200;i++){const r=plinkoResult();assert.equal(r.path.length,8);assert(r.path.every(x=>x===0||x===1));assert.equal(r.slot,r.path.reduce((a,b)=>a+b,0));assert.equal(r.multiplier,multipliers[r.slot]);assert(['heads','tails'].includes(coinResult()));}});
test('Arcade rejects anonymous and cross-origin writes before database access',async()=>{for(const action of ['create','plinko','accept','cancel','start','move']){let r=response();await api({method:'POST',headers:{origin:'https://www.zeekfusion.com'},body:{action}},r);assert.equal(r.statusCode,401);r=response();await api({method:'POST',headers:{origin:'https://evil.example'},body:{action}},r);assert.equal(r.statusCode,403);}});
import {diceResult,nextCard,mineBoard} from '../server/arcade.mjs';
import {amountZ} from '../server/z-format.mjs';
test('dice outcomes are valid and never tied; cards stay within the published deck',()=>{for(let i=0;i<200;i++){const [a,b]=diceResult();assert(a>=1&&a<=6&&b>=1&&b<=6&&a!==b);const c=nextCard();assert(c>=1&&c<=13);}});
test('minefields have distinct in-range cells for every allowed mine count',()=>{for(let n=1;n<=20;n++){const cells=mineBoard(n);assert.equal(cells.length,n);assert.equal(new Set(cells).size,n);assert(cells.every(c=>c>=0&&c<25));}for(const n of [0,21,1.5,'3',null])assert.throws(()=>mineBoard(n));});
test('currency uses Z only at exactly one',()=>{assert.equal(amountZ(1),'1 Z');assert.equal(amountZ('1.00'),'1 Z');assert.equal(amountZ(0),'0 Zs');assert.equal(amountZ(1.5),'1.5 Zs');assert.equal(amountZ(-1),'-1 Zs');});
