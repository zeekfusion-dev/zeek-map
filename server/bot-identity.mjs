import {BROADCASTER} from './domain.mjs';
export const botName=()=>process.env.KICK_BOT_USERNAME||'ZeekBot';
export function botCredentials(){const client_id=process.env.KICK_BOT_CLIENT_ID,client_secret=process.env.KICK_BOT_CLIENT_SECRET;if(!client_id||!client_secret)throw new Error('Set up the dedicated ZeekBot OAuth app before connecting.');return {client_id,client_secret};}
export function verifyBotIdentity(user,expectedId){const id=Number(user?.user_id);if(!Number.isSafeInteger(id)||id<=0||id===BROADCASTER||typeof user?.name!=='string'||user.name.toLowerCase()!==botName().toLowerCase()||(expectedId&&id!==Number(expectedId))||(process.env.KICK_BOT_USER_ID&&id!==Number(process.env.KICK_BOT_USER_ID)))throw new Error('Authorize the dedicated '+botName()+' account, not your streamer account.');return {user_id:id,name:user.name};}
export function validateBotGrant(t){const app=botCredentials();if(t?.purpose!=='dedicated-chat-bot'||t.client_id!==app.client_id||!t.access_token||!t.refresh_token||!Number.isFinite(t.expires_at))throw new Error('Connect '+botName()+' in Owner Controls. Streamer credentials cannot send bot messages.');verifyBotIdentity(t.identity);return t;}
